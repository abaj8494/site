<script src="{{ "js/collapsible-global.js" | relURL }}" defer></script>

<style>
  /* Wrapper for heading and toggle button */
  .collapsible-wrapper {
      display: flex;
      align-items: center;
      margin-bottom: 0px;
  }

  /* Toggle button */
  .toggle-button {
      cursor: pointer;
      border: none;
      background: none;
      color: black;
      font-size: 1rem;
      width: 1.5rem; /* Ensure consistent alignment */
      height: 1.5rem;
      text-align: center;
      line-height: 1.5rem;
      margin-right: 8px;
      padding: 0;
  }

  [data-theme="dark"] .toggle-button {
      color: white; /* Override to white when theme is dark */
  }

  /* Remove outline when focused */
  .toggle-button:focus {
      outline: none;
  }

  /* Collapsible content (hidden by default) */
  .collapsible-content {
      display: none;
      margin-left: 15px; /* Indent to align with heading */
      margin-bottom: 0px;
      padding: 1px 0;
      border-left: 0px solid #ccc;
      transition: all 0.3s ease-out;
  }

  /* Content visible when toggled */
  .collapsible-content.show {
      display: block;
  }
</style>

<div class="collapsible-wrapper">
  <button class="toggle-button" aria-expanded="{{ if .Get "folded" }}false{{ else }}true{{ end }}">
    {{ if .Get "folded" }}➖{{ else }}➕{{ end }}
  </button>
  <div class="collapsible-content {{ if .Get "folded" }}{{ "folded" }}{{ end }}">
    {{ .Inner }}
  </div>
</div>
